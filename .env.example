# Example environment variables for BulenCoin stack

#########################
# BulenNode (bulennode/)
#########################

# Node profile: desktop-full | server-full | mobile-light | tablet-light | raspberry | gateway
BULEN_NODE_PROFILE=server-full

# Security
BULEN_REQUIRE_SIGNATURES=true
BULEN_ENABLE_FAUCET=false
# Shared secret for P2P HTTP traffic between trusted nodes
BULEN_P2P_TOKEN=replace-with-strong-secret

# Networking
BULEN_HTTP_PORT=4100
BULEN_P2P_PORT=4101
BULEN_MAX_BODY_SIZE=128kb
BULEN_CORS_ORIGINS=

# Protocol compatibility (optional, defaults to package version)
BULEN_PROTOCOL_VERSION=1.0.0

#########################
# Explorer (explorer/)
#########################

EXPLORER_PORT=4200
BULENNODE_API_BASE=http://localhost:4100/api
EXPLORER_TITLE=BulenCoin Explorer
EXPLORER_LOG_FORMAT=dev

#########################
# Status service (status/)
#########################

STATUS_PORT=4300
# Comma-separated list of BulenNode /api/status endpoints
STATUS_NODES=http://localhost:4100/api/status

